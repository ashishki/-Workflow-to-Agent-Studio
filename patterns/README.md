# Pattern Library

Pattern files are local Markdown templates used as retrieval context for blueprint structure, approval boundaries, eval cases, integration checks, and guardrail examples.

Current templates:

- `patterns/automation_blueprint.md`
- `patterns/eval_cases.md`

Keep pattern content generic. Do not include credentials, client secrets, or private source text.
